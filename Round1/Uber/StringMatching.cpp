/*
String pattern matching
The matching should cover the entier input string (not partial).
The function prototype should be 
bool ismatch(String str, String pattern)
isMatch("aa", "a")->false
isMatch("aa", "aa")->true
isMatch("aa", "a{1, 3}")->true
isMatch("abcc", "a{1, 3}b{1, 3}cc{1, 3}")->true
in pattern string, a char followed by {lower, upper}means that
the char occur lower to upper(execlusive) times, a{1, 3} a occurs 1 or 2 times
*/
#include <string>

bool isMatch(string &s, string &p) {
    
}
